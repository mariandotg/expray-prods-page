import styled from "styled-components";

const SectionTitle = styled.p`
  font-weight: 900;
  font-size: 2rem;
  line-height: 2rem;
  align-self: flex-start;
  align-text: left;
`;

export { SectionTitle };
